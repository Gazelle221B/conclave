# 設計: `<機能名>`  (参照: REQUIREMENTS.md)

> 作成者: `ARCHITECT`。**これは下流全員が読む単一の真実の源 (SSOT)。** 曖昧な記述・先送り表現を残さない。
> 実装は行わない。設計判断とリスク評価がスコープ。

## 設計方針

採用するアプローチの要約 (3-5 行)。

## 検討した代替案と採用理由

| 案 | 長所 | 短所 | 採否 |
|---|---|---|---|
| | | | |

## API / インターフェース境界

公開する関数 / エンドポイント / 型のシグネチャ。

## データ構造・スキーマ

主要なデータモデルと永続化方針。一意制約・キー生成順を明記 (→ 憲法 C-2 の根拠)。

## 変更が及ぶ範囲

影響するモジュール / ファイル群。

## リスクと緩和策

技術 / セキュリティ / 並行性 / 性能上の懸念と対策。

## 実装上の禁止事項 (§7)

> 実装者が作業前に必ず読み返すセクション。憲法 (CONSTITUTION) の該当項目を具体化する。

- やってはいけないこと (後方互換破壊、特定 API の直接呼び出し、スコープ外機能の先回り実装 等)。

## 非機能要件への対応 (§8)

要件の非機能項目 (性能・セキュリティ・並行性・可用性・コスト上限 等) に対する具体策。

## オープン論点

不確実な点を明示し、判断を後続スプリント/人間へ委ねる。重大決定は **ADR** を別ファイルで残しここからリンク。

> **曖昧さの明示規約**: 設計中の未確定点は推測で埋めず `[NEEDS CLARIFICATION: <質問>]` でインライン明示する。
> **完了ゲート**: 「`[NEEDS CLARIFICATION]` マーカーが残っていない」ことが実装着手の前提(曖昧さゼロを検出可能なトークンで担保する)。
